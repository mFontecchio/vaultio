# Copy to keystore.properties (gitignored) for local signed release/nightly builds.
# Paths are relative to the repo root unless absolute.

storeFile=vaultio-upload.jks
storePassword=CHANGE_ME
keyAlias=vaultio
keyPassword=CHANGE_ME
